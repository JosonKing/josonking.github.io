import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Joson',
  mode: 'site',
  locales: [
    ['zh-CN', '中文'],
    ['en-US', 'English'],
  ],
  // more config: https://d.umijs.org/config
});
